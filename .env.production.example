# Production Environment Variables (Banner Deployment)
#
# IMPORTANT: This deploys to Banner (10.0.0.33), NOT localhost!
#
# To get the database password:
#   source ~/Infrastructure/scripts/secrets.sh
#   appservices_get POSTGRES_PASSWORD

# Database - runs on Banner
DB_HOST=db
DB_PORT=5432
DB_NAME=stock_researcher
DB_USER=postgres
DB_PASSWORD=<get from appservices_get POSTGRES_PASSWORD>

# Server
PORT=3157
NODE_ENV=production
LOG_LEVEL=info
CORS_ORIGIN=https://stock-researcher.nextlevelfoundry.com

# Client (built into static files, not used at runtime)
VITE_API_URL=https://stock-researcher.nextlevelfoundry.com/api
